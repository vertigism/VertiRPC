bin/
obj/
publish/
installer/Output/

# WPF writes a temporary project beside the real one for the markup pass and
# deletes it afterwards; an interrupted build leaves one behind.
*_wpftmp.csproj

*.user
*.suo

.vs/
.vscode/

# The PyQt build that 2.0.0 replaced, kept on disk for reference only.
VertiRPC.old/
